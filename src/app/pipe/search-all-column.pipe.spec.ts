import { SearchAllColumnPipe } from './search-all-column.pipe';

describe('SearchAllColumnPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchAllColumnPipe();
    expect(pipe).toBeTruthy();
  });
});
